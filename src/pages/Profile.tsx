import { FC, memo } from "react";

/**
 * ==> props interface
 */
interface IProps {

}

/**
 * ==> Component
 */
const Profile: FC<IProps> = ({  }) => {
  return (
    <>

    </>
  );
}

export default memo(Profile);