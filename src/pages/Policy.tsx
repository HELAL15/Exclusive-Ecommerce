import { FC, memo } from "react";

/**
 * ==> props interface
 */
interface IProps {

}

/**
 * ==> Component
 */
const Policy: FC<IProps> = ({  }) => {
  return (
    <>

    </>
  );
}

export default memo(Policy);