import { FC, memo } from "react";

/**
 * ==> props interface
 */
interface IProps {

}

/**
 * ==> Component
 */
const Terms: FC<IProps> = ({  }) => {
  return (
    <>

    </>
  );
}

export default memo(Terms);