import { FC, memo } from "react";

/**
 * ==> props interface
 */
interface IProps {

}

/**
 * ==> Component
 */
const Returns: FC<IProps> = ({  }) => {
  return (
    <>

    </>
  );
}

export default memo(Returns);